test <- function() {success("Very nice! Flip back and forth between the last two plots you made.  Note the `fs` model has fewer wild swings - it is more stable for models where some categories have few data points, though we don't get statistics on the individual smooths in this case.")}

